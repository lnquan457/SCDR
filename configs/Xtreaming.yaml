exp_params:
  dataset: 'isolet_subset'
  input_dims: 617   # x for vectors or (h, w, c) for images
  latent_dim: 2
  stream_rate: 100
  indices_file_path: -1
  initial_data_num: 300

method_params:
  buffer_size: 100
  eta: 0.99